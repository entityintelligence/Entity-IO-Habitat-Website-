# KIT / Entity io

Marketing site for Kinetic Information Technology.

```bash
npm install
npm run dev
```

Waitlist posts to Resend when `RESEND_API_KEY` is set (see `.env.example`). Otherwise submissions are stored in `data/waitlist.json` during local development.
