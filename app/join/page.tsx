import { JoinStage } from "@/components/join-stage";

export const metadata = {
  title: "Request a seat — KIT",
  description: "Request a seat for KIT by Entity io. Design partner or waitlist.",
};

export default function JoinPage() {
  return <JoinStage />;
}
